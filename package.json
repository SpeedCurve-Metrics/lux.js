{
  "name": "lux.js",
  "scripts": {
    "build": "mkdir -p dist && terser --mangle --compress --output dist/lux.min.js src/lux.js",
    "jest": "jest",
    "jest-watch": "jest --watch",
    "prettier": "prettier --write 'tests/**/*.js'",
    "eslint": "eslint 'src/**/*.js' 'tests/**/*.js'",
    "test": "npm run eslint && npm run jest"
  },
  "devDependencies": {
    "eslint": "^7.12.1",
    "eslint-config-prettier": "^6.15.0",
    "jest": "^26.6.2",
    "jest-puppeteer": "^4.4.0",
    "prettier": "^2.1.2",
    "puppeteer": "^5.4.1",
    "rimraf": "^3.0.2",
    "terser": "^5.3.8"
  },
  "prettier": {
    "printWidth": 100
  }
}
